# Bluetooth Scanner Dependencies
bleak>=0.21.0              # BLE scanning library (cross-platform)
flask>=3.0.0               # Web framework
flask-socketio>=5.3.0      # Real-time WebSocket support
python-socketio>=5.10.0    # Socket.IO implementation
simple-websocket>=1.0.0    # WebSocket support for threading mode (replaces eventlet)
gevent>=24.0.0             # Alternative async support (fallback)
gevent-websocket>=0.10.1   # WebSocket support for gevent
requests>=2.31.0           # HTTP requests for OUI lookups

# Testing
pytest>=8.0.0              # Test framework
pytest-asyncio>=0.23.0     # Async test support
pytest-cov>=4.1.0          # Coverage reporting
